<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bbct_app_admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.member_pr.PreviewPrActivity"></activity>
        <activity android:name=".ui.member_nsr.PreviewActivity" />
        <activity android:name=".view.main.MainDeletePostActivity" />
        <activity android:name=".ui.post.PostCommentActivity" />
        <activity android:name=".ui.post.PostActivity" />
        <activity android:name=".view.main.MainInsertActivity" />
        <activity android:name=".view.main.MainDeleteActivity" />
        <activity android:name=".view.main.MainEditActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>